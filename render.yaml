services:
  - type: web
    name: deckwash
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn deckwash:app --workers 2 --threads 4 --timeout 300 --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
